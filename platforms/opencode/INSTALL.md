# OpenCode — Install Guide

## Quick Install

```bash
bash scripts/install.sh opencode
```

## MCP Setup (Optional)

Run `cd scripts && python3 setup.py` first, then configure the MCP server in OpenCode settings.
