# Windsurf — Install Guide

## Quick Install

```bash
bash scripts/install.sh windsurf
```

## MCP Setup (Optional)

Configure the `google-ads-mcp` server in Windsurf MCP settings.

Run `cd scripts && python3 setup.py` first.
